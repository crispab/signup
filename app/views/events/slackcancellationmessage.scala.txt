@(event: models.Event, baseURL: String)
@import org.json.JSONObject._
  {
     "attachments" : [
        {
           "fallback" : @quote("Inställd sammankomst: <" + baseURL + routes.Events.show(event.id.get) + "|" + event.group.mailSubjectPrefix + ": " + event.name + ">!"),
           "pretext" : @quote("Inställd sammankomst: <" + baseURL + routes.Events.show(event.id.get) + "|" + event.group.mailSubjectPrefix + ": " + event.name + ">!"),
           "color" : "#EE3C0C",
           "fields" : [
              {
                 "title" : "Tid",
                 "value" : "@util.DateHelper.asDateTime(event.startTime)-@util.DateHelper.asTime(event.endTime)",
                 "short" : false
              },
              {
                 "title" : "Orsak",
                 "value" : @quote(event.cancellationReason.getOrElse("Ej angiven")),
                 "short" : false
              }
           ]
        }
     ]
  }
