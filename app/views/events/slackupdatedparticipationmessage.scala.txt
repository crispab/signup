@(event: models.Event, participation: models.Participation, baseURL: String)
@import org.json.JSONObject._
  {
     "attachments" : [
        {
           "fallback" : @quote("Uppdaterad anmälan i sammankomsten <" + baseURL + routes.Events.show(event.id.get) + "|" + event.group.mailSubjectPrefix + ": " + event.name + ">!"),
           "pretext" : @quote("Uppdaterad anmälan i sammankomsten <" + baseURL + routes.Events.show(event.id.get) + "|" + event.group.mailSubjectPrefix + ": " + event.name + ">!"),
           "color" : "#00759F",
           "fields" : [
              @if(participation.numberOfParticipants > 1) {
                {
                   "title" : @quote(participation.user.name),
                   "value" : @quote(util.StatusHelper.asMessage(participation.status) + " " + participation.numberOfParticipants + " i sällskapet"),
                   "short" : false
                }
              } else {
                {
                   "title" : @quote(participation.user.name),
                   "value" : @quote(util.StatusHelper.asMessage(participation.status)),
                   "short" : false
                }
              }
              @if(!participation.comment.isEmpty) {
                ,{
                   "title" : "Kommentar",
                   "value" : @quote(participation.comment),
                   "short" : false
                }
              }
           ]
        }
     ]
  }
